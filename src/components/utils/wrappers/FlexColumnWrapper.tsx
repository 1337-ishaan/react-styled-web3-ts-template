import styled from "styled-components";
import FlexRowWrapper from "./FlexRowWrapper";

const FlexColumnWrapper = styled(FlexRowWrapper)`
  flex-direction: column;
`;

export default FlexColumnWrapper;
